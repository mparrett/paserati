{
  "comment": [
    "The pinned benchmark corpus. perf-session.sh measures an engine commit with",
    "the benchmarks named here, taken from `ref`, instead of the ones that commit",
    "happened to ship with.",
    "",
    "Why: without this, benchmark drift is confounded with engine change. Two",
    "commits whose bench_test.go differs are not measuring the same thing, and a",
    "commit predating a benchmark cannot be measured by it at all — which is what",
    "makes historical re-measurement impossible and forces an epoch break every",
    "time a benchmark is fixed.",
    "",
    "Only _test.go files and tests/scripts fixtures may appear in `files`.",
    "bench-corpus.sh refuses anything else, because overlaying engine source",
    "would measure a different engine than the commit under test.",
    "",
    "Version history — numbers are comparable only within a version:",
    "  v2  first pinned corpus.",
    "  v3  escape the accumulators in the pkg/vm micros (IsObject, ToInteger,",
    "      GetOwn) via runtime.KeepAlive. Under v2 the IsObject loop body was",
    "      dead-code eliminated at commits where IsObject inlines and retained",
    "      where it does not, so v2 IsObject deltas across that boundary",
    "      overstate the gain and must not be compared against v3.",
    "  v4  add the seven Octane workloads. This is a version bump and not a",
    "      quiet addition because pinning splits a cell into separate go test",
    "      invocations, so changing which benchmarks share a process can move",
    "      the existing ones. Costs ~22s on top of a ~7min cell.",
    "",
    "Octane's RegExp workload is deliberately absent: it fails its own checksum",
    "on every engine across the measured window, so it is a bug to fix rather",
    "than a benchmark to run."
  ],
  "version": "v4",
  "ref": "perf/bench-corpus-v2",
  "files": [
    "tests/bench_test.go",
    "tests/benchmark_prototype_test.go",
    "tests/scripts/bench_add.ts",
    "tests/scripts/bench_arith.ts",
    "tests/scripts/bench_setindex.ts",
    "tests/scripts/factorial.ts",
    "tests/scripts/matrix_mult.ts",
    "pkg/vm/bench_ratchet_anchor_test.go",
    "pkg/vm/isobject_bench_test.go",
    "pkg/vm/object_bench_test.go",
    "pkg/vm/tointeger_bench_test.go",
    "tests/bench_octane_test.go",
    "tests/scripts/octane/LICENSE",
    "tests/scripts/octane/richards.js",
    "tests/scripts/octane/richards-run.js",
    "tests/scripts/octane/deltablue.js",
    "tests/scripts/octane/deltablue-run.js",
    "tests/scripts/octane/crypto.js",
    "tests/scripts/octane/crypto-run.js",
    "tests/scripts/octane/raytrace.js",
    "tests/scripts/octane/raytrace-run.js",
    "tests/scripts/octane/earley-boyer.js",
    "tests/scripts/octane/earley-boyer-run.js",
    "tests/scripts/octane/splay.js",
    "tests/scripts/octane/splay-run.js",
    "tests/scripts/octane/splay-teardown.js",
    "tests/scripts/octane/navier-stokes.js",
    "tests/scripts/octane/navier-stokes-run.js",
    "tests/scripts/octane/navier-stokes-teardown.js"
  ]
}
